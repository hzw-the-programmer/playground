sudo apt-get install build-essential

switch in same app:
alt + ~

switch between different apps:
alt + tab

maximize window:
alt + f10

df -h

ls -lhd work
du -hs work

du -h --max-depth=1 . | sort -hr
