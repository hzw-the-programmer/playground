# 逻辑复制与流复制协议 (简要)

用于主备物理复制和逻辑复制订阅。消息模型基于 Copy 协议进行封装，**walsender** 进程使用以下关键流程：

- 客户端发送 `IDENTIFY_SYSTEM` 获取系统信息。

- 根据时间线、LSN 位置发送 `START_REPLICATION` 命令。

- 服务器通过 **CopyData** 持续发送 WAL 记录。

- 逻辑复制使用发布/订阅，涉及快照构建和事务解码消息。
