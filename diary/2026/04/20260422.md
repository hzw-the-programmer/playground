# 日常
- 学 gitoxide

  ```Rust
  decode::commit.parse_next(input)
  ```
  commit是个方法：
  ```Rust
  pub fn commit<'a, E: ParserError<&'a [u8]> + AddContext<&'a [u8], StrContext>>(
    i: &mut &'a [u8],
  ) -> ModalResult<CommitRef<'a>, E> {}
  ```
  commit方法为啥会有parse_next方法？因为：
  ```Rust
  impl<I, O, E, F> Parser<I, O, E> for F
  where
      F: FnMut(&mut I) -> Result<O, E>,
      I: Stream,
  ```

- 学 winnow
  
  D:\cargo_home\registry\src\index.crates.io-1949cf8c6b5b557f\winnow-1.0.1\src\_tutorial\chapter_2.rs

- 跟着 豆包、deepseek 学 clippy

  Carnet Ginza（カルネ ギンザ）
  《黑色皮革手册》（黒革の手帖）
  原口元子（武井咲 饰）
