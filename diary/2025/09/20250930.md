大跌的时候不买，大涨的时候不卖，就会导致在价格不合适的时候买卖。

看tokio的task

看麦当劳致股东信
https://corporate.mcdonalds.com/content/dam/sites/corp/nfl/pdf/McD%20-%202024%20CEO%20Letter.pdf
麦当劳系统
建立对话、驱动参与
每天负担的起的菜单
在全球规模化好点子

We have the right playbook in Accelerating the
Arches. We have the right advantages: our size,
our scale, our brand relevance. We have the right
mindset and a strong legacy of acting on our biggest
and boldest ideas. And we have the power of our
three-legged stool.

不知道正确的事是什么，就不做错误的事。

block_pool最大线程数是：阻塞线程数 + 工作线程数
```rust
impl Builder {
    fn build_threaded_runtime(&mut self) -> io::Result<Runtime> {
        use crate::loom::sys::num_cpus;

        let worker_threads = self.worker_threads.unwrap_or_else(num_cpus);

        // Create the blocking pool
        let blocking_pool =
            blocking::create_blocking_pool(self, self.max_blocking_threads + worker_threads);
        let blocking_spawner = blocking_pool.spawner().clone();
    }
}
```

工作线程只跑一个阻塞任务：
```rust
impl Launch {
    pub(crate) fn launch(mut self) {
        for worker in self.0.drain(..) {
            runtime::spawn_blocking(move || run(worker));
        }
    }
}
```

UnownedTask::run调用的是raw::poll，也就是BlockingTask::poll。
BlockingTask是一个future，被包装到UnownedTask。